#item-delete-modal.modal.fade{ tabindex: "-1", role: "dialog", "aria-labelledby" => "item-delete-modal-label"}
  .modal-dialog{role: "document"}
    .modal-content
      .modal-header
        %button.close{type: "button", "data-dismiss" => "modal", "aria-label" => "Close"}
          %span{"aria-hidden" => "true"}
            &times
        %h4#item-delete-modal-label.modal-title
          Do you want to delete this visualization?
      .modal-body
        %p This visualization appears also in:
        -# TODO! Add stories where appear the current visualization
        %p Be careful, deleting can't be undone.

        %p
          %strong
            %i Are you sure?
        = button_to "Yes", @visualization, :method => :delete, :class => "btn btn-danger"
        %button.btn.btn-default{type: "button", "data-dismiss" => "modal", "aria-label" => "Close"}
          Cancel